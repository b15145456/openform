apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: openform
  namespace: openform
spec:
  podSelector:
    matchLabels: { app: openform }
  policyTypes: [Ingress]
  ingress:
    - ports:
        - { protocol: TCP, port: 8080 }
